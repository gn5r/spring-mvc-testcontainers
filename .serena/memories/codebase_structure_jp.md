
* 標準的なMavenプロジェクトのレイアウト。
* `src/main/java` にJavaコード。
* `src/main/resources` にリソース。
* `src/main/webapp` にWebリソース。
* `src/test` にテスト。
* アプリケーションは、機能 (`employee`, `index`) とレイヤー (`controller`, `service`, `mapper`, `dto`, `form`) によって構造化されています。
